{
  "version": 2,
  "framework": "vite",
  "buildCommand": "npm run build-web",
  "devCommand": "npm run dev",
  "builds": [
    {
      "src": "main.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/main.py"
    },
    {
      "src": "/name/(.*)",
      "dest": "/main.py"
    },
    {
      "src": "/coords/(.*)/(.*)",
      "dest": "/main.py"
    }
  ],
  "env": {
    "APP_MODULE": "main:app"
  }
}